#include "main.h"

/**
 * _strlen - a function to print the lenth of a string
 *
 * @s: the parameter that contains the string
 *
 * Return: the length of thr string (i)
 *
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
