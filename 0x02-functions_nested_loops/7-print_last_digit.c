#include "main.h"

/**
 * print_last_digit - function that prints the last digit of a given number
 *
 * @c: the given number
 *
 * Return: always lastDigit which is the last digit
 *
 */
int print_last_digit()
{
	int c;
	int lastDigit = c % 10;

	return (lastDigit);
}
